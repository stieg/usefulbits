# Bash ENV settings

chkExists () {
   which $1 &>/dev/null
}

if chkExists emacs; then
    EDITOR='emacs -nw'
elif chkExists nano; then
    EDITOR='nano'
else
    EDITOR='vim'
fi

# Setup a local bin directory
export PATH="$PATH:$HOME/bin"
